services:
  mongo:
    image: mongo:7
    restart: always
    ports: ["27017:27017"]
    volumes:
      - mongo_data:/data/db

  interactions:
    build: .
    restart: always
    ports: ["3000:3000"]
    env_file: .env
    depends_on: [mongo]

volumes:
  mongo_data:
